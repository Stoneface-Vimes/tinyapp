<!DOCTYPE html>
<html land="en">

<head>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
    integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>URL - TinyApp Example</title>
</head>

<% if (!user) { %>
<!-- Display when the client is not logged in -->

<body>
  <%- include("./partials/_header"); -%>
  <main style="margin: 1em;">
    <h3>Hello Stranger!</h3>
    <i>You're not currently logged in... 3:</i><br>
    <i>Please click register if you do not have an account</i><br>
    <i>Or hit login to enter your credentials and see your URLS</i>
  </main>
  <% } else if (user === 'thief') { %>
  <!-- Display when the client is logged in and the tinyurl they are viewing is not theirs -->

  <body>
    <%- include("./partials/_header"); -%>
    <main style="margin: 1em;">
      <h3>This isn't yours!</h3>
      <i>Your account is not able to view tinyURLS you did not make</i>
    </main>
    <% } else { %>
    <!-- Display when the client is logged in and viewing tinyurls they own -->
    <!-- Displays the user ID, the tiny url as a clickable link, a field containing the longurl and a button to update the longurl -->
    <body>
      <%- include("./partials/_header"); -%>
      <div class="card text-center">
        <div class="card-header"></div>
        <div class="card-body">
          <h5 class="card-title">User ID : <%= user.id %></h5>
          <p class="card-text">Tiny URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a></p>
          <form method="POST" action="/urls/<%= shortURL %>">Update Long URL? <input type="text" name="newLongURL"
              value=<%= longURL %>><input type="submit" value="Submit">
          </form>
        </div>
        <div class="card-footer text-muted"></div>
      </div>
      <% } %>
      <!-- Bootstrap JS -->
      <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
        integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
        crossorigin="anonymous"></script>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"
        integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1"
        crossorigin="anonymous"></script>
      <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"
        integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM"
        crossorigin="anonymous"></script>
    </body>

</html>